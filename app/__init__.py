"""SC.AI application package.

The `app` package holds the shared helpers (scai_common), the browser
registry (browsers), the Selenium runtime (seleniumTest) and the frontend
(index.html). Both the launcher and the runtime import from this package.
"""